package edu.uw.ruc.web;

import java.io.IOException;

import javax.servlet.Filter;
import javax.servlet.FilterChain;
import javax.servlet.FilterConfig;
import javax.servlet.ServletException;
import javax.servlet.ServletRequest;
import javax.servlet.ServletResponse;

public abstract class JsonRequestFilter implements Filter {
   /** FilterConfig cfg;

    
    public JsonRequestFilter() {
    }

    public void destroy() {
    }
    public void doFilter(final ServletRequest request, final ServletResponse response, final FilterChain chain) throws IOException, ServletException {
    
    ServiceRequest requestWrapper = request;
    if("appliaction/json".equals(request.getContentType())&& request instanceof HttpServlet*/
}
